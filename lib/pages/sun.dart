import 'package:flutter/material.dart';

class SunPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Sun')),
      body: Center(child: Text('No data yet.')),
      //body: Center(child: Text('Sun')),
    );
  }
}
